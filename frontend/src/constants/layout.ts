/**
 * Maximum content width used across the application for full-width pages
 * (dashboards, lists, job boards, etc.).
 *
 * Use this constant in:
 *  - Container size prop on pages
 *  - MainHeader inner Container (so the nav aligns with page content)
 *
 * Narrower pages (forms, detail views) intentionally use smaller sizes
 * like "sm", "md", or "lg" and should NOT use this constant.
 */
export const APP_MAX_WIDTH = 1440;
